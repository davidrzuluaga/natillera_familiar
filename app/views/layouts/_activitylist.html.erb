<table class="table">
        <thead>
          <tr>
            <th scope="col">Fecha</th>
            <th scope="col">Actividad</th>
            <th scope="col">Ganancia Total</th>
            <th scope="col">Encargado</th>
          </tr>
        </thead>
    <tbody>
        <%@activitylist.each do |act|%>
        <tr>
          <th><%= link_to "&#128465;".html_safe, activitylistdelete_path(act), method: :delete, data: { confirm: "¿Eliminar?" } if user_signed_in? && current_user.admin?%> <%=I18n.l act.date%></th>
          <td><%=act.name%></td>
          <td><%=number_to_currency(Activity.where(activity: act.name).sum(:earn), :unit => "$")%></td>
          <td><%=act.person%></td>
        </tr>
        <%end%>
    </tbody>
</table>